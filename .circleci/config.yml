version: 2.1
workflows:
  version: 2
  build_push_test:
    jobs:
      # Eventually this job should come from a CircleCI maintained orb.
      - build_and_deploy
jobs:
  build_and_deploy:
    docker:
      - image: node:10.15.3
    steps:
    - checkout
    - run: npm ci
    - run: npm build
  
